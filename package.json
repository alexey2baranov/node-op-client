{
  "version": "1.0.20",
  "_from": "op-client",
  "author": {
    "name": "alexey2baranov@gmail.com"
  },
  "dependencies": {
    "client-oauth2": "^4.3.3",
    "cross-fetch": "^3.0.5",
    "iso8601-duration": "^1.2.0",
    "keypather": "^3.1.0",
    "reflect-metadata": "^0.1.10"
  },
  "description": "client library for OpenProject.org server. Supports Node.js and browsers.",
  "devDependencies": {
    "@types/jest": "^26.0.9",
    "@types/node": "^8.10.64",
    "@zerollup/ts-transform-paths": "^1.7.18",
    "dotenv": "^8.2.0",
    "jest": "^26.4.0",
    "ts-jest": "^26.2.0",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^3.9.7"
  },
  "keywords": [
    "openproject",
    "client",
    "server"
  ],
  "license": "MIT",
  "main": "dist/index.js",
  "name": "op-client",
  "scripts": {
    "build": "tsc",
    "master": "git checkout master && git merge development && git push && git checkout development",
    "test": "jest  --runInBand",
    "test:server:down": "docker stop node-op-client-test && docker rm node-op-client-test",
    "test:server:up": "docker run -it -p 8091:80 --name node-op-client-test -v /var/openproject/pgdata -v /var/openproject/assets openproject/community:10"
  }
}
